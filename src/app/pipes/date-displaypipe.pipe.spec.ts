import { DateDisplaypipePipe } from './date-displaypipe.pipe';

describe('DateDisplaypipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateDisplaypipePipe();
    expect(pipe).toBeTruthy();
  });
});
